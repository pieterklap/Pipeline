# Options that start with a # are set to default
#
#  EXCLUDE 
#  exclude deltaCn*, Mascot*, and Comet* results from results (default Penalize * results)

#  LEAVE 
#  leave alone deltaCn*, Mascot*, and Comet* results from results (default Penalize * results)

#  PERFECTLIB 
#  multiply by SpectraST library probability (default NO)

#  ICAT 
#  apply ICAT model (default Autodetect ICAT)

#  NOICAT 
#  do no apply ICAT model (default Autodetect ICAT)

#  ZERO 
#  report results with minimum probability 0 (default 0.05)

#  ACCMASS 
#  use Accurate Mass model binning (default NO)

#  CLEVEL=0 
#  set Conservative Level in neg_stdev from the neg_mean, low numbers are less conservative, high numbers are more conservative (default 0)

#  PPM 
#  use ppm mass error instead of Daltons for mass modeling (default Daltons)

#  NOMASS 
#  disable mass model (default NO)

#  MASSWIDTH=5.0 
#  mass model model mass width (default 5.0)

#  PI 
#  enable peptide pI model (default NO)

#  MINPINTT=2 
#  minimum number of NTT in a peptide used for positive pI model (default 2)

#  MINPIPROB=0.9 
#  minimum probability after first pass of a peptide used for positive pI model (default 0.9)

#  RT 
#  enable peptide RT model (default NO)

#  MINRTNTT=2 
#  minimum number of NTT in a peptide used for positive RT model (default 2)

#  MINRTPROB=0.9 
#  minimum probability after first pass of a peptide used for positive RT model (default 0.9)

#  RTCAT=<rtcatalog file> 
#  enable peptide RT model, use <rtcatalog_file> peptide RTs when available as the theoretical value

#  RTCOEF=<rtcalc_coefficients file> 
#  enable peptide RT model, use <rtcalc_coefficient_file> to predict peptide RTs as the theoretical value

#  GLYC 
#  enable peptide Glyco motif model (default NO)

#  PHOSPHO 
#  enable peptide Phospho motif model (default NO)

#  MALDI 
#  enable MALDI mode (default NO)

#  INSTRWARN 
#  warn and continue if combined data was generated by different instrument models (default NO)

#  MINPROB=0.05 
#  report results with minimum probability <number> (default 0.05)

#  DECOY=\###RND###
#  semi-supervised mode, protein name prefix to identify Decoy entries

#  DECOYPROBS 
#  compute possible non-zero probabilities for Decoy entries on the last iteration 

#  IGNORECHG=<charge> 
#  can be used multiple times to specify all charge states to exclude from modeling

#  NONTT 
#  disable NTT enzymatic termini model (default NO)

#  NONMC 
#  disable NMC missed cleavage model (default NO)

#  EXPECTSCORE 
#  use expectation value as the only contributor to the f-value for modeling (default NO)

#  NONPARAM 
#  use semi-parametric modeling, must be used in conjunction with DECOY= option (default NO)

#  NEGGAMMA 
#  use Gamma distribution to model the negative hits

#  FORCEDISTR 
#  bypass quality control checks, report model despite bad modelling

#  OPTIMIZEFVAL 
#  (SpectraST only) optimize f-value function f(dot,delta) using PCA (default NO)
